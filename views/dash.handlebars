<section class="container">
  <h3 class="topic-name">Dashboard</h3>
  <section class="flex-row justify-center">
    {{#each topic.posts as |post|}}
    {{> post-titles}}
    {{/each}}
  </section>
    <p>dash.handlebars</p>

</section>