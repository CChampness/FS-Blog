<section class="card">
  <a href="/topic/{{id}}">
    <header>Topic: {{name}}</header>
    <p>Most recent post for this topic:</p>
    <hr>
    {{posts.[0].content}}
  </a>
  <p>(topic-details.handlebars)</p>
</section>