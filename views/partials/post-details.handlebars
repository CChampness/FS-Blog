<section class="card">
  <a href="/post/{{id}}">
    <header>{{title}}</header>
<h1>post-details.handlebars</h1>
    <div class="card-body">
      <p>{{content}}</p>
      <p>By: {{blogger}}</p>
    </div>
  </a>
    <p>post-details.handlebars</p>

</section>