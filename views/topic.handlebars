<section class="container">
  <h3 class="topic-name">{{topic.name}}</h3>
  <section class="flex-row justify-center">
    {{#each topic.posts as |post|}}
    {{> post-details}}
    {{/each}}
  </section>
    <p>(topic.handlebars)</p>

</section>