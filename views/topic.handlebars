<section class="container">
  <h3 class="topic-name">{{topic.name}} Posts</h3>
  <section class="flex-row justify-center">
    {{#each topic.posts as |post|}}
    {{> post-details}}
    {{/each}}
  </section>
</section>